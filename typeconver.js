// type conversion = change the datatype to another 
//              (strings, number , boolean)


// 1. conversion of string to number
let age = window.prompt("How old are you");

age = Number(age);

age +=1;

console.log(age, typeof age); 

//2.  conversion of number to string
let x = window.prompt("How old are you");

x += 20;

console.log(x, typeof x);

